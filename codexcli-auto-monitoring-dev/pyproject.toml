[project]
name = "codexcli-auto-monitoring-dev"
version = "0.1.0"
description = "Step-by-step implementation workspace for Codex CLI auto monitoring runner"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

